{
  "schema": 1,
  "type": "completion",
  "description": "Gets the intent from a description",
  "execution_settings": {
    "default": {
      "max_tokens": 1000,
      "temperature": 0
    }
  },
  "input_variables": [
    {
      "name": "$request",
      "description": "A description of an action the user would like to perform",
      "required": true
    }
  ]
}